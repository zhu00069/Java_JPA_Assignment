CREATE TABLE EMPLOYEE (ID INTEGER IDENTITY NOT NULL, FIRSTNAME VARCHAR, LASTNAME VARCHAR, SALARY DOUBLE, VERSION INTEGER, ADDRESS_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE ADDRESS (ID INTEGER IDENTITY NOT NULL, CITY VARCHAR, COUNTRY VARCHAR, POSTAL VARCHAR, STATE VARCHAR, STREET VARCHAR, VERSION INTEGER, PRIMARY KEY (ID))
CREATE TABLE PHONE (ID INTEGER IDENTITY NOT NULL, AREACODE VARCHAR, PHONENUMBER VARCHAR, TYPE VARCHAR, VERSION INTEGER, OWNING_EMP_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE PROJECT (ID INTEGER IDENTITY NOT NULL, DESCRIPTION VARCHAR, NAME VARCHAR, VERSION INTEGER, PRIMARY KEY (ID))
CREATE TABLE EMP_PROJ (EMP_ID INTEGER NOT NULL, PROJ_ID INTEGER NOT NULL, PRIMARY KEY (EMP_ID, PROJ_ID))
ALTER TABLE EMPLOYEE ADD CONSTRAINT FK_EMPLOYEE_ADDR_ID FOREIGN KEY (ADDRESS_ID) REFERENCES ADDRESS (ID)
ALTER TABLE PHONE ADD CONSTRAINT FK_PHONE_OWNING_EMP_ID FOREIGN KEY (OWNING_EMP_ID) REFERENCES EMPLOYEE (ID)
ALTER TABLE EMP_PROJ ADD CONSTRAINT FK_EMP_PROJ_PROJ_ID FOREIGN KEY (PROJ_ID) REFERENCES PROJECT (ID)
ALTER TABLE EMP_PROJ ADD CONSTRAINT FK_EMP_PROJ_EMP_ID FOREIGN KEY (EMP_ID) REFERENCES EMPLOYEE (ID)